**Input** — frosted-glass text field. Focus swaps the frost hairline for the violet focus ring.

```jsx
<Input placeholder="Buscar extrações…" icon={<Icon name="search" size={18} />} />
```

Sits on the elevated surface; pairs with `Icon` for search and form fields.
