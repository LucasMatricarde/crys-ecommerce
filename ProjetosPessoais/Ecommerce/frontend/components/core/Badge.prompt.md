**Badge** — tiny mono counter pill for cart quantities and notifications.

```jsx
<Badge tone="amber">2</Badge>
<Badge tone="frost">9+</Badge>
```

Tones: `amber` (default), `frost` (neutral), `violet`. Number renders in Space Mono.
