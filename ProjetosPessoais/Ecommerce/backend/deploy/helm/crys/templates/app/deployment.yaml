{{- /* One Deployment per entry in .Values.services (DRY, see _helpers.tpl). */ -}}
{{- range $name, $svc := .Values.services }}
---
{{ include "crys.appDeployment" (dict "root" $ "name" $name "svc" $svc) }}
{{- end }}
