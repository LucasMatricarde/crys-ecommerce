package com.crys.notification.domain;

/** The customer-facing notification kinds CRYS. sends across the order lifecycle. */
public enum NotificationType {
    ORDER_RECEIVED,
    ORDER_CONFIRMED,
    ORDER_CANCELLED
}
